# Detecting sudoku grid without OpenCV (prototype)

Using UNET and very few samples (about 50) turns out to be sufficient for the class segmentation of acceptable quality without any algorithmic aid.

**Required units:**

- PIL
- PyTorch
- torchvision
- numpy
- Matplotlib
